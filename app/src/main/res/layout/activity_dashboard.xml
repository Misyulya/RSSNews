<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include layout="@layout/toolbar" />


    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        tools:context=".activities.DashboardActivity">


        <Button
            android:id="@+id/rssButton"
            style="@style/ButtonStyle"
            android:layout_alignParentStart="true"
            android:text="@string/rss_button" />

        <Button
            android:id="@+id/backupButton"
            style="@style/ButtonStyle"
            android:layout_toRightOf="@+id/rssButton"
            android:text="@string/backup_button" />

        <Button
            android:id="@+id/restoreButton"
            style="@style/ButtonStyle"
            android:layout_below="@+id/rssButton"
            android:text="@string/restore_button" />

        <Button
            android:id="@+id/exitButton"
            style="@style/ButtonStyle"
            android:layout_below="@+id/backupButton"
            android:layout_toEndOf="@+id/restoreButton"
            android:text="@string/exit_button" />


    </RelativeLayout>
</LinearLayout>
